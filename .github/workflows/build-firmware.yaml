name: build-firmware

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  schedule:
    - cron:  '0 1 * * *'
  workflow_dispatch:

jobs:
  build-firmware:

    name: build-firmware
    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Checkout required submodules
      run: |
        git submodule update --init --recursive --depth 1 device/nodemcu/nodemcu-firmware

    - name: Get nodemcu-firmware sha
      id: get-firmware-sha
      run: |
        echo "sha=$(git rev-parse HEAD:device/nodemcu/nodemcu-firmware)" >> $GITHUB_OUTPUT
      shell: bash

    - name: Cache Espressif toolchain
      uses: actions/cache@v3
      with:
        path: device/nodemcu/.espressif
        key: ${{ runner.os }}-espressif-toolchain-${{ steps.get-firmware-sha.outputs.sha }}

    - name: Build
      run: |
        device/nodemcu/build.sh

    - name: Archive the firmware
      uses: actions/upload-artifact@v2
      with:
        path: device/nodemcu/build/firmware-esp32.zip
        if-no-files-found: error
