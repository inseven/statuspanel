arduino
saleae
StatusPanel.xcodeproj/xcuserdata
node_modules
apns.p8
.DS_Store
console.log
xcuserdata
.env*
/.local
/build
/firmware/build
/ios/StatusPanel/configuration.json

# Site
_site
.jekyll-cache

# Website
# Right now we can't actually ignore these as there's no way to force add them using git-auto-commit.
# /docs/simulator
# /docs/release-notes
